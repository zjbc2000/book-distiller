# 第二章精要：命名规范

> 来源：第二章 命名规范

## 核心规则
- 函数名以动词开头：动作即名字，如 getUser、fetchData、calcTotal。
- 布尔变量用 is/has 开头：如 isReady、hasChildren，让条件判断读起来像自然语言。
- 避免无意义缩写：缩写要能「读出来是什么」，无意义缩写破坏可读性。

## 实践提示（When → use）
- 当命名一个函数 → 以动词开头，表达它「做什么」。
- 当命名一个布尔值 → 用 is/has 前缀，表达「是否为某状态」。
- 当想用缩写 → 先问缩写是否无歧义、可读，否则写全称。
